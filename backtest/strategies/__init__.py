"""backtest/strategies/ — logiques de signal spécifiques à chaque stratégie candidate, écrites
pour être consommées par `backtest/engine.py` (jamais l'inverse : ce paquet ne simule aucun
portefeuille lui-même, cf. docstring de `backtest/engine.py`)."""
